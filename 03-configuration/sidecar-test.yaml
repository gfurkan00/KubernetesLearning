apiVersion: v1
kind: Pod
metadata:
  name: sidecar-test
spec:
  containers:
  - name: app
    image: nginx:1.27
  initContainers:
  - name: my-sidecar
    image: busybox:1.36
    restartPolicy: Always
    command: ['sh','-c','while true;do echo "sidecar alive"; sleep 1; done']
